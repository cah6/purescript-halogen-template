{
  "private": true,
  "scripts": {
    "build": "spago build",
    "bundle": "spago bundle-app --main Main --to docs/app.js",
    "serve": "yarn bundle && parcel build docs/index.html && parcel serve docs/index.html --open & sass --watch --sourcemap=none docs/custom.scss:docs/generated.css"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "pulp": "^12.3.0",
    "purescript": "^0.13.2",
    "spago": "^0.8.5"
  }
}
